#!/usr/bin/env python3
"""示例数据层：这张最小导图的全部内容都写在这里，结构怎么拼由构建脚本负责。

这就是「数据/构建分离」的那一半——改内容只动这个文件，不碰 .xmind 成品，
也不手工拼 content.json。词条（TERMS）活在数据层，构建时按「词的每个使用点」
自动附成笔记（标题里出现该词的节点就带上它的释义小树）。
"""

# 根节点 = 这张图的主体，一句话钉死
ROOT = "示例导图"

# 词条表：词 -> 一段话释义。释义里用到的其它词条必须也在表里（链闭），且不许互相指回（无环）。
TERMS = {
    "验收线": "验一张图合不合规的唯一关口，结构面与内容面两道一起过才算交付",
    "闸门": "机器拦违规的检查，不过就不出图",
    "构建脚本": "把数据层确定性地拼成图的程序，同一输入永远得到同一张图",
}

# 释义之间的依赖：谁的释义里用到了别的词条，谁的行就缩进挂在引入者下面
TERM_DEPS = {
    "验收线": ["闸门"],
    "闸门": [],
    "构建脚本": ["闸门"],
}

# 内容源：[标题, [孩子]]，每支平铺直挂
TREE = [
    ROOT,
    [
        ["是什么", [["一句主张", []], ["支撑事实", []], ["例子占一层", []]]],
        ["怎么跑", [["内容进数据文件", []], ["结构进构建脚本", []], ["机器检查进闸门", []]]],
        ["验收线", [["先过结构校验", []], ["再过内容检查", []], ["红点不许盖", []]]],
    ],
]

# 结构计划表：每个一级块必须挂全这些孩子标题——漏挂或多挂都在构建期报错，
# 而不是等出图后人肉发现。
PLANS = {
    "是什么": ["一句主张", "支撑事实", "例子占一层"],
    "怎么跑": ["内容进数据文件", "结构进构建脚本", "机器检查进闸门"],
    "验收线": ["先过结构校验", "再过内容检查", "红点不许盖"],
}

if __name__ == "__main__":
    print("这是数据层；出图请跑 python examples/example_build.py")
