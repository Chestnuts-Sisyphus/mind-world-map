## What changed

<!-- One paragraph: which standard, gate, tool or document moved, and why. -->

## Evidence

- [ ] `python tools/preflight.py` exits 0
- [ ] `python tools/preflight.py --self-test` reports PASS for every category
- [ ] `python tools/sync_check.py` exits 0 (or reports "no local master, skipped")
- [ ] Added a CHANGELOG entry under `[Unreleased]`
- [ ] Standards changed only in `SKILL.md`, not inside a deliverable

## Does this soften any rule?

<!-- If yes, stop: propose the standard change separately, with the evidence that the
     current rule is wrong. Softening a budget to let one artifact through has been
     tried and reverted twice. -->

## Linked issues

Closes #
